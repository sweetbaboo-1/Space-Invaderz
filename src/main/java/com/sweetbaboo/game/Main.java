package com.sweetbaboo.game;

// import java.awt.*;

public class Main {
  // custom screen size because it's so much easier...
  public static final int SCREEN_WIDTH = 1500; // (int) Toolkit.getDefaultToolkit().getScreenSize().getWidth();
  public static final int SCREEN_HEIGHT = 1000; // (int) Toolkit.getDefaultToolkit().getScreenSize().getHeight();
  public static final int FRAME_DELAY = 16;
  public static void main(String[] args) {
    new Frame();
  }
}
